---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cycle_dns_zone Data Source - cycle"
subcategory: ""
description: |-
  Looks up a Cycle DNS zone by ID or by origin (domain name). Exactly one of id or origin must be set.
---

# cycle_dns_zone (Data Source)

Looks up a Cycle DNS zone by ID or by origin (domain name). Exactly one of `id` or `origin` must be set.

## Example Usage

```terraform
# Look up a DNS zone by its origin (domain name)...
data "cycle_dns_zone" "by_origin" {
  origin = "example.com"
}

# ...or by its ID.
data "cycle_dns_zone" "by_id" {
  id = "651586fca6078e98982dbd90"
}

output "zone_hosted" {
  value = data.cycle_dns_zone.by_origin.hosted
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (String) The unique ID of the DNS zone. Conflicts with `origin`.
- `origin` (String) The origin (domain name) of the DNS zone, e.g. `example.com`. Conflicts with `id`.

### Read-Only

- `hosted` (Boolean) Whether this zone is hosted by Cycle (`true`) or linked (`false`).
- `state` (String) The current state of the DNS zone.
