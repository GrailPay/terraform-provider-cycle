---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cycle_environment Resource - cycle"
subcategory: ""
description: |-
  Manages a Cycle environment. Environments are groups of containers with a private network built between them, deployed into a cluster.
---

# cycle_environment (Resource)

Manages a Cycle environment. Environments are groups of containers with a private network built between them, deployed into a cluster.

## Example Usage

```terraform
resource "cycle_cluster" "production" {
  identifier = "production"
}

resource "cycle_environment" "api" {
  name        = "API"
  cluster     = cycle_cluster.production.identifier
  description = "Production API environment"

  # Optional; auto-generated from the name if omitted.
  identifier = "api"

  # Create-only: changing this forces a new environment.
  legacy_networking = false
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `cluster` (String) The identifier of the cluster this environment is deployed into. Changing this forces a new environment to be created.
- `name` (String) A user-defined name for the environment.

### Optional

- `description` (String) A custom description for the environment.
- `identifier` (String) A human-readable slugged identifier for the environment. Automatically generated from the name if not provided.
- `legacy_networking` (Boolean) Whether legacy networking mode is enabled on this environment. Can only be set at creation time; changing this forces a new environment to be created. Defaults to `false`.

### Read-Only

- `hub_id` (String) The ID of the hub this environment belongs to.
- `id` (String) The unique ID of the environment.
- `state` (String) The current state of the environment (e.g. `live`).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Environments are imported using their ID.
terraform import cycle_environment.api 651efd54c53f7b6e2c5a9f21
```
