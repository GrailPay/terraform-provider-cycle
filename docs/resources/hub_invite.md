---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cycle_hub_invite Resource - cycle"
subcategory: ""
description: |-
  Sends an invitation to join the current Cycle hub (/v1/hubs/current/invites). Invites cannot be modified after they are sent, so any change forces a new invite. Once the recipient accepts (or declines) the invite, it disappears from the hub's pending invites and this resource is removed from state on the next refresh; use cycle_hub_member to manage the resulting membership.
---

# cycle_hub_invite (Resource)

Sends an invitation to join the current Cycle hub (`/v1/hubs/current/invites`). Invites cannot be modified after they are sent, so any change forces a new invite. Once the recipient accepts (or declines) the invite, it disappears from the hub's pending invites and this resource is removed from state on the next refresh; use `cycle_hub_member` to manage the resulting membership.

## Example Usage

```terraform
resource "cycle_hub_role" "developer" {
  name       = "Developer"
  identifier = "developer"
  rank       = 3

  capabilities = {
    specific = [
      "environments-view",
      "containers-view",
      "containers-manage",
    ]
  }
}

# Invite a teammate to the hub with the developer role. The invite is
# revoked if the resource is destroyed before it is accepted. Once accepted,
# the invite disappears and the membership can be managed with
# cycle_hub_member.
resource "cycle_hub_invite" "teammate" {
  recipient = "teammate@example.com"
  role_id   = cycle_hub_role.developer.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `recipient` (String) The email address of the person to invite. Changing this forces a new invite.
- `role_id` (String) The ID of the hub role the invitee will be assigned upon accepting. Changing this forces a new invite.

### Read-Only

- `id` (String) The unique ID of the invite (a pending hub membership ID).
- `state` (String) The current state of the invite membership (e.g. `pending`).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import a pending hub invite by its ID (the invite membership ID).
terraform import cycle_hub_invite.teammate 651f1e95c3f1b2a4d8e7a456
```
