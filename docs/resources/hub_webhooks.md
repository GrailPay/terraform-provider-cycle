---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cycle_hub_webhooks Resource - cycle"
subcategory: ""
description: |-
  Manages hub-level webhooks on the current Cycle hub (PATCH /v1/hubs/current). This is a singleton — Terraform state is the source of truth, and create applies the configured URLs rather than creating a separate API resource. Destroy clears both webhook URLs.
---

# cycle_hub_webhooks (Resource)

Manages hub-level webhooks on the current Cycle hub (`PATCH /v1/hubs/current`). This is a singleton — Terraform state is the source of truth, and create applies the configured URLs rather than creating a separate API resource. Destroy clears both webhook URLs.

## Example Usage

```terraform
# Hub webhooks are a singleton on the current hub.
resource "cycle_hub_webhooks" "current" {
  server_deployed = "https://hooks.example.com/cycle/server-deployed"
  server_deleted  = "https://hooks.example.com/cycle/server-deleted"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `server_deleted` (String) Webhook URL invoked whenever a server in this hub is deleted. The payload is a `Server` object. Omit or set to `""` to unset.
- `server_deployed` (String) Webhook URL invoked whenever a server is deployed to this hub. The payload is a `Server` object. Omit or set to `""` to unset.

### Read-Only

- `id` (String) The ID of the current hub.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import using the hub ID, or the literal "current".
terraform import cycle_hub_webhooks.current current
terraform import cycle_hub_webhooks.current 651efd54c53f7b6e2c5a9f00
```
