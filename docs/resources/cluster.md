---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "cycle_cluster Resource - cycle"
subcategory: ""
description: |-
  Manages a Cycle infrastructure cluster. Clusters are groups of servers that allow physical separation of resources; environments are deployed into a cluster.
---

# cycle_cluster (Resource)

Manages a Cycle infrastructure cluster. Clusters are groups of servers that allow physical separation of resources; environments are deployed into a cluster.

## Example Usage

```terraform
resource "cycle_cluster" "production" {
  identifier = "production"
}

resource "cycle_cluster" "staging" {
  identifier = "staging"

  # Non-essential clusters are excluded by default from certain
  # metrics and summaries.
  non_essential = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `identifier` (String) A human-readable slugged identifier for the cluster (e.g. `production`). Changing this forces a new cluster to be created.

### Optional

- `non_essential` (Boolean) Marks the cluster as non-essential. Non-essential cluster resources are excluded by default from certain metrics and summaries unless opted in. Defaults to `false`.

### Read-Only

- `hub_id` (String) The ID of the hub this cluster belongs to.
- `id` (String) The unique ID of the cluster.
- `state` (String) The current state of the cluster (e.g. `live`).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Clusters are imported using their ID.
terraform import cycle_cluster.production 651efd54c53f7b6e2c5a9f21
```
